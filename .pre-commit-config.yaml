repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: pytest
        args: ["-q", "--tb=short", "tests/"]
        language: system
        pass_filenames: false
        always_run: true
