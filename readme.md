Leitor de MDB para ORM ObjectBox / Flutter

# Como usar?

Abra o terminal na pasta do projeto e rode o comando:
```bash
python main.py
```

Depois escolha em File -> Load Object Box Schema -> Carregue o arquivo objectbox-model.json do seu projeto
